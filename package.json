{
  "name": "quran-learning-app",
  "private": true,
  "description": "Flutter app + React web UI. Run web scripts from repo root or from web/.",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "preview": "npm run preview --prefix web",
    "sync-assets": "npm run sync-assets --prefix web"
  }
}
